# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
.env
.env.*

# Jupyter
.ipynb_checkpoints/

# Project outputs
# Keep runs ignored by default (large, many files)
outputs/runs/*
!outputs/runs/.gitkeep

# Track plots and gallery images
# (no ignore rule for outputs/plots)

# Data
# Track content and style images in repo for reproducibility
# (Remove ignore rules so files under data/content and data/style are committed)

# OS / Editor
.DS_Store
Thumbs.db
*.swp
.vs/
.vscode/
